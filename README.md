# 🚗 國道五號 (雪山隧道) 智慧車流決策系統 (Web Service)

一個基於現代 C++ 開發的高效能網頁服務，透過後端代理 (Backend Proxy) 機制串接交通部 TDX API，即時分析雪山隧道內外車道的平均車速，並為駕駛提供最佳車道選擇建議。

## 🌟 專案亮點

本專案從單機版進化為 **Web Service 架構**，解決了前端直接呼叫 API 易洩漏金鑰的問題，並實現了伺服器端快取。

* **🔒 後端代理架構**：使用 C++ 作為中間層伺服器，隱藏 API Key，確保資安無虞。
* **⚡ 伺服器端快取 (Server-side Caching)**：內建 60 秒快取機制，大幅降低對 TDX API 的請求頻率，避免觸發 Rate Limit。
* **🚀 高效能 C++ 網頁框架**：採用 **Crow** (微型 C++ Web 框架) 與 **Asio** 非同步網路庫，輕量且快速。
* **☁️ 雲端部署就緒**：支援 Windows 本地開發與 Google Cloud Platform (GCP) Linux 環境部署。
* **🧠 智慧決策演算法**：自動計算雪隧主線 (15K-28K) 全路段加權平均車速，提供「內側 vs 外側」最佳路徑建議。

## 🏗️ 系統架構

```mermaid
graph LR
    User[使用者瀏覽器] -- HTTP GET --> Server[C++ Crow Server]
    Server -- 檢查快取 --> Cache{快取有效?}
    Cache -- Yes --> Server
    Cache -- No --> TDX[交通部 TDX API]
    TDX -- JSON Data --> Server
    Server -- 分析與決策 --> User

